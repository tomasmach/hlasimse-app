# Simulator run evidence — YYYY-MM-DD / COMMIT

Status: **NOT RUN**

- Git commit:
- Tester:
- Django/database configuration:
- iOS simulator model, OS, UDID:
- Android AVD, API level, serial:
- iOS raw artifact directory/checksum:
- Android raw artifact directory/checksum:
- iOS JUnit result:
- Android JUnit result:
- Failed/retried steps:
- Screenshots reviewed:
- Known limitations:
- Physical-device evidence references:
- Release decision and approver:

Do not change the status to passed unless every flow has a zero exit code and its raw artifacts were
reviewed. Record partial runs as partial; never infer a platform result from the other platform.
