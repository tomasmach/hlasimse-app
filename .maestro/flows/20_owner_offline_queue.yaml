appId: ${APP_ID}
name: Owner sees an unconfirmed request when the API is unavailable
tags:
  - simulator
  - offline
---
- tapOn:
    id: "checkin-submit"
- extendedWaitUntil:
    visible: "Čeká na připojení — strážci zatím spoléhají na původní termín"
    timeout: 25000
- assertVisible: "1 hlášení čeká na odeslání.*"
- assertVisible:
    id: "offline-queue-sync"
